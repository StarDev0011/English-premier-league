import { NavBarWrapper } from "./styles"

export default function Nav() {
    const navItems = ['Home', 'About', 'Contact'];
    return (
        <NavBarWrapper>
            <h1>Nav</h1>
        </NavBarWrapper>
    )
}